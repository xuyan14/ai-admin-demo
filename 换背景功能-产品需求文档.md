# 换背景功能 - 产品需求文档（PRD）

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| **产品名称** | AI内容管理后台 - 智能换背景功能 |
| **功能模块** | 创意工具 > 换背景 > 自定义背景图 |
| **版本号** | v1.0 |
| **创建日期** | 2025-10-09 |
| **状态** | ✅ 已实现 |
| **核心技术** | NanoBanana AI绘画API |

---

## 🎯 产品概述

### 1.1 产品定位

为电商运营人员提供基于AI的智能换背景工具，通过NanoBanana API实现商品图背景的智能替换，支持两种模式：
1. **背景生成模式** - 通过文字描述生成新背景
2. **背景上传模式** - 使用用户上传的真实背景图

### 1.2 核心价值

- 🚀 **提升效率** - 自动化背景替换，节省人工抠图和合成时间
- 🎨 **创意无限** - AI生成任意风格的背景
- 🎯 **精准控制** - 支持上传特定背景图实现精确替换
- 💰 **降低成本** - 无需专业设计师，运营人员即可完成

### 1.3 目标用户

- 📦 电商运营人员
- 🎨 内容创作者
- 📸 商品摄影师
- 🛍️ 营销推广人员

---

## 📊 功能需求

### 2.1 功能架构

```
换背景功能
├── 背景图库模式（现有功能）
│   └── 从预设背景图库中选择
│
└── 自定义背景图模式 ⭐ NanoBanana集成
    ├── 2.2.1 背景生成（默认）
    │   └── 通过文字描述AI生成背景
    │
    └── 2.2.2 背景上传
        └── 上传真实背景图进行替换
```

---

### 2.2 核心功能详述

#### **2.2.1 背景生成模式**

**功能描述：**

用户通过自然语言描述期望的背景，AI根据描述生成背景并替换商品图的背景。

**交互流程：**

```
Step 1: 选择商品图
  ↓
Step 2: 选择背景图比例（1:1、3:4、9:16等）
  ↓
Step 3: 点击"自定义背景图"标签
  ↓
Step 4: 确保"背景生成"tab被选中（默认）
  ↓
Step 5: 输入背景描述
  - 方式1：手动输入描述
  - 方式2：点击预设示例按钮快速填充
  ↓
Step 6: 点击"生产"按钮
  ↓
Step 7: 系统调用NanoBanana API
  ↓
Step 8: 显示生成结果
```

**输入要素：**

| 要素 | 必填 | 说明 | 示例 |
|------|------|------|------|
| 商品图 | ✅ | 需要换背景的商品图片 | 商品主图URL |
| 背景描述 | ✅ | 文字描述期望的背景 | "蓝天白云的户外场景，阳光明媚" |
| 背景比例 | ✅ | 生成图片的比例 | 1:1、3:4、9:16 |

**Prompt构建规则：**

```
最终Prompt = "请帮我替换图片背景。背景为" + {用户输入的背景描述}
```

**示例：**
- 用户输入："蓝天白云的户外场景，阳光明媚，清新自然"
- 最终Prompt："请帮我替换图片背景。背景为蓝天白云的户外场景，阳光明媚，清新自然"

**预设示例提示词：**

1. **户外场景** - "清新自然的户外场景，蓝天白云，阳光明媚，绿色草地"
2. **室内场景** - "现代简约室内背景，白色墙面，明亮光线，干净整洁"
3. **渐变色彩** - "渐变色彩背景，从浅蓝到深蓝，柔和过渡，简约时尚"
4. **纯色背景** - "纯色背景，高级灰色调，柔和光影，商务风格"

**API调用参数：**

```json
{
  "image_url": "商品图URL",
  "image_urls": ["商品图URL"],
  "prompt": "请帮我替换图片背景。背景为{背景描述}",
  "model": "nb",
  "function": "change-background",
  "bg_ratio": "1:1",
  "bg_method": "generate"
}
```

**成功标准：**

- ✅ 商品主体保持完整清晰
- ✅ 背景符合描述要求
- ✅ 商品与背景自然融合
- ✅ 图片比例符合选择
- ✅ 10-30秒内生成完成

---

#### **2.2.2 背景上传模式**

**功能描述：**

用户上传一张真实的背景图片，AI将商品图的背景替换为上传的背景图，实现精确的场景合成。

**交互流程：**

```
Step 1: 选择商品图
  ↓
Step 2: 选择背景图比例（1:1、3:4、9:16等）
  ↓
Step 3: 点击"自定义背景图"标签
  ↓
Step 4: 点击"背景上传"tab切换模式
  ↓
Step 5: 上传背景图
  - 点击上传区域
  - 选择本地图片文件
  - 等待上传完成（显示预览）
  ↓
Step 6: 点击"生产"按钮
  ↓
Step 7: 系统调用NanoBanana API（传递2张图片）
  ↓
Step 8: 显示生成结果
```

**输入要素：**

| 要素 | 必填 | 说明 | 限制 |
|------|------|------|------|
| 商品图 | ✅ | 需要换背景的商品图片 | - |
| 背景图 | ✅ | 用户上传的背景图 | JPEG/JPG/PNG/WEBP，≤10M |
| 背景比例 | ✅ | 生成图片的比例 | 1:1、3:4、9:16 |

**Prompt构建规则：**

```
固定Prompt = "请将第一张图的背景替换为第二张图"
```

**图片传递顺序：**
```
image_urls = [商品图URL, 背景图URL]
            ↑          ↑
          第一张      第二张
```

**API调用参数：**

```json
{
  "image_url": "商品图URL",
  "image_urls": ["商品图URL", "背景图URL"],
  "prompt": "请将第一张图的背景替换为第二张图",
  "model": "nb",
  "function": "change-background",
  "bg_ratio": "1:1",
  "bg_method": "upload"
}
```

**上传流程：**

```
用户选择文件
  ↓
前端验证（格式、大小）
  ↓
POST /api/upload-image
  ↓
上传到腾讯云COS
  ↓
返回公网URL
  ↓
显示预览 + 绿色提示："✓ 已上传：{URL前缀}..."
  ↓
保存到 uploadedBgImageUrl 变量
```

**成功标准：**

- ✅ 商品主体保持完整
- ✅ 背景完全替换为上传的背景图
- ✅ 商品与背景透视、光影合理
- ✅ 图片比例符合选择
- ✅ 10-30秒内生成完成

---

### 2.3 通用功能需求

#### **2.3.1 结果展示**

**展示内容：**
- 生成的换背景图片（最大宽度400px）
- 生成时间
- 使用的Prompt

**操作按钮：**
- **在新窗口打开** - 在新标签页查看原图
- **下载图片** - 保存到本地
- **重新生成** - 使用相同参数重新生成

**界面布局：**
```
┌──────────────────────────────────┐
│ 换背景结果 - 自定义背景  时间   │
│                      [重新生成]  │
├──────────────────────────────────┤
│                                  │
│        ┌──────────┐             │
│        │          │             │
│        │ 结果图片  │  ← 400px    │
│        │          │             │
│        └──────────┘             │
│                                  │
│  [在新窗口打开] [下载图片]       │
│                                  │
│  Prompt: 请帮我替换图片背景...  │
│                                  │
└──────────────────────────────────┘
```

---

#### **2.3.2 加载状态**

**加载中界面：**
```
┌──────────────────────────────────┐
│                                  │
│           ⏳                     │
│   正在使用NB模型生成换背景...    │
│                                  │
│   背景描述: 蓝天白云的户外场景   │
│                                  │
│   ┌────────────────────────┐   │
│   │ 🔍 调用Prompt:          │   │
│   │ 请帮我替换图片背景。     │   │
│   │ 背景为蓝天白云的户外...  │   │
│   │                         │   │
│   │ 📷 图片数量: 2张         │   │  ← 仅背景上传模式
│   │ (商品图+背景图)          │   │
│   └────────────────────────┘   │
│                                  │
└──────────────────────────────────┘
```

**Toast提示：**
- "🚀 开始调用NB模型API换背景..."
- "✅ 换背景生成成功！"
- "❌ 换背景失败: {错误信息}"

---

#### **2.3.3 错误处理**

**前端验证：**
1. 商品图未选择 → "请先选择商品图片"
2. 背景比例未选择 → "请选择背景图比例"
3. 背景生成：描述为空 → "请输入背景描述"
4. 背景上传：未上传背景图 → "请先上传背景图"

**API错误：**
1. 网络错误 → "网络连接失败，请检查网络"
2. API错误 → 显示具体错误信息
3. 超时 → "请求超时，请重试"

**错误界面：**
```
┌──────────────────────────────────┐
│           ❌                     │
│     换背景生成失败               │
│                                  │
│   {错误信息}                     │
│                                  │
│         [重试]                   │
│                                  │
└──────────────────────────────────┘
```

---

## 🔧 技术需求

### 3.1 NanoBanana API集成

#### **3.1.1 API端点**

**基础信息：**
- **API地址：** `https://grsai.dakka.com.cn`
- **接口：** `/v1/draw/nano-banana`
- **方法：** POST
- **认证：** Bearer Token

#### **3.1.2 请求格式**

**背景生成模式：**
```json
{
  "prompt": "请帮我替换图片背景。背景为蓝天白云的户外场景，阳光明媚，清新自然",
  "image_urls": [
    "https://12131231-1302391623.cos.ap-beijing.myqcloud.com/商品图.jpg"
  ],
  "model": "nano-banana-fast"
}
```

**背景上传模式：**
```json
{
  "prompt": "请将第一张图的背景替换为第二张图",
  "image_urls": [
    "https://12131231-1302391623.cos.ap-beijing.myqcloud.com/商品图.jpg",
    "https://12131231-1302391623.cos.ap-beijing.myqcloud.com/背景图.jpg"
  ],
  "model": "nano-banana-fast"
}
```

#### **3.1.3 响应格式**

**成功响应：**
```json
{
  "id": "task-uuid",
  "status": "succeeded",
  "results": [
    {
      "url": "https://file3.grsai.com/file/xxx.png"
    }
  ],
  "progress": 100
}
```

**处理中响应：**
```json
{
  "id": "task-uuid",
  "status": "processing",
  "progress": 50
}
```

**失败响应：**
```json
{
  "status": "failed",
  "error": "错误描述",
  "failure_reason": "详细原因"
}
```

---

### 3.2 系统架构

```
┌─────────────┐
│  前端页面    │
│ (HTML/JS)   │
└──────┬──────┘
       │ HTTP请求
       ↓
┌─────────────┐
│ 后端API     │
│ (Flask)     │
│ Python 3.9  │
└──────┬──────┘
       │ HTTP请求
       │ + API Key
       ↓
┌─────────────┐
│ NanoBanana  │
│ AI API      │
└──────┬──────┘
       │
       ↓
┌─────────────┐
│ 生成结果    │
│ (图片URL)   │
└─────────────┘
```

---

### 3.3 后端API设计

#### **3.3.1 换背景接口**

**端点：** `POST /api/change-background`

**请求参数：**

| 参数 | 类型 | 必填 | 说明 | 示例 |
|------|------|------|------|------|
| image_url | string | ✅ | 商品图URL | "https://..." |
| image_urls | array | ✅ | 图片URL列表 | ["商品图", "背景图"] |
| prompt | string | ✅ | AI处理指令 | "请帮我替换图片背景..." |
| model | string | ✅ | 模型名称 | "nb" |
| bg_ratio | string | ✅ | 背景比例 | "1:1" |
| bg_method | string | ✅ | 背景方法 | "generate" / "upload" |

**响应格式：**

**成功：**
```json
{
  "success": true,
  "status": "succeeded",
  "data": {
    "output_url": "https://file3.grsai.com/file/xxx.png",
    "task_id": "uuid",
    "prompt": "使用的Prompt"
  }
}
```

**处理中：**
```json
{
  "success": true,
  "status": "processing",
  "data": {
    "task_id": "uuid",
    "message": "图片正在生成中，请稍候..."
  }
}
```

**失败：**
```json
{
  "success": false,
  "error": "错误描述",
  "status": "failed"
}
```

---

### 3.4 图片上传服务

#### **3.4.1 上传接口**

**端点：** `POST /api/upload-image`

**请求格式：** `multipart/form-data`

**请求参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| image | file | ✅ | 图片文件 |

**文件限制：**
- **格式：** JPEG、JPG、PNG、WEBP
- **大小：** ≤ 10MB
- **用途：** 上传背景图，获取公网URL

**上传流程：**
```
1. 接收文件
2. 验证格式和大小
3. 生成唯一文件名（timestamp_uuid.ext）
4. 上传到腾讯云COS（公有桶）
5. 返回公网URL
```

**响应格式：**
```json
{
  "success": true,
  "data": {
    "url": "https://12131231-1302391623.cos.ap-beijing.myqcloud.com/nanobanana/web/xxx.jpg",
    "filename": "20251009_120000_abc123.jpg",
    "original_name": "background.jpg",
    "upload_type": "cos"
  }
}
```

---

### 3.5 数据流转

#### **背景生成模式数据流：**

```
用户输入描述
  ↓
前端构建请求
  ↓
POST /api/change-background
  ↓
后端验证参数
  ↓
调用NanoBanana API
  image_urls: [商品图]
  prompt: "请帮我替换图片背景。背景为{描述}"
  ↓
NanoBanana生成图片
  ↓
返回output_url
  ↓
前端显示结果
```

#### **背景上传模式数据流：**

```
用户选择文件
  ↓
POST /api/upload-image
  ↓
上传到COS获取URL
  ↓
前端保存URL + 显示预览
  ↓
用户点击"生产"
  ↓
POST /api/change-background
  ↓
后端验证参数
  ↓
调用NanoBanana API
  image_urls: [商品图, 背景图]
  prompt: "请将第一张图的背景替换为第二张图"
  ↓
NanoBanana生成图片
  ↓
返回output_url
  ↓
前端显示结果
```

---

## 📐 界面设计

### 4.1 Tab切换设计

**一级Tab（背景来源）：**
```
┌────────────────────────────────┐
│ [选择背景图] [自定义背景图 ✓]  │
└────────────────────────────────┘
```

**二级Tab（自定义方式）：**
```
┌────────────────────────────────┐
│ [背景生成 ✓] [背景上传]        │
└────────────────────────────────┘
```

**样式规范：**
- **选中状态：** 背景#1890ff，文字白色
- **未选中状态：** 背景白色，文字#666
- **过渡动画：** 0.3s

---

### 4.2 背景生成界面

```
┌────────────────────────────────┐
│ [背景生成 ✓] [背景上传]        │
├────────────────────────────────┤
│                                │
│ 背景描述 (Prompt)              │
│ ┌────────────────────────────┐│
│ │请描述您想要的背景图...      ││
│ │                            ││
│ │                            ││
│ └────────────────────────────┘│
│ ℹ️ 提示：详细的描述...  0/500  │
│                                │
│ 示例提示词                     │
│ [户外场景] [室内场景]          │
│ [渐变色彩] [纯色背景]          │
│                                │
└────────────────────────────────┘
```

---

### 4.3 背景上传界面

```
┌────────────────────────────────┐
│ [背景生成] [背景上传 ✓]        │
├────────────────────────────────┤
│                                │
│ 上传背景图                     │
│ ┌────────────────────────────┐│
│ │         ☁️                  ││
│ │   点击上传背景图            ││
│ │ JPEG/JPG/PNG/WEBP 10M内    ││
│ └────────────────────────────┘│
│                                │
│ ℹ️ 提示：上传的背景图将替换   │
│    商品图的背景                │
│                                │
└────────────────────────────────┘
```

**上传成功后：**
```
┌────────────────────────────────┐
│ [背景生成] [背景上传 ✓]        │
├────────────────────────────────┤
│                                │
│ 上传背景图                     │
│ ┌────────────────────────────┐│
│ │ [预览图]      [重新上传]   ││
│ └────────────────────────────┘│
│ ✓ 已上传：https://12131231... │  ← 绿色确认
│                                │
│ ℹ️ 提示：上传的背景图将替换   │
│    商品图的背景                │
│                                │
└────────────────────────────────┘
```

---

## 🎯 用户体验需求

### 5.1 反馈机制

**即时反馈：**
- 上传背景图：Toast提示"📤 正在上传..." → "✅ 背景图上传成功！"
- 开始生成：Toast提示"🚀 开始调用NB模型API..."
- 生成成功：Toast提示"✅ 换背景生成成功！"
- 生成失败：Toast提示"❌ 换背景失败: {原因}"

**进度反馈：**
- 显示加载动画
- 显示实时状态文字
- 显示使用的Prompt信息

**结果反馈：**
- 显示生成的图片
- 显示生成时间
- 提供操作按钮

---

### 5.2 容错设计

**网络异常：**
- 自动重试机制（可选）
- 友好的错误提示
- 提供重试按钮

**API限流：**
- 显示"服务繁忙，请稍后重试"
- 建议用户等待后重试

**图片格式错误：**
- 前端预验证
- 明确提示支持的格式
- 阻止不支持的格式上传

---

### 5.3 性能要求

| 指标 | 目标 | 说明 |
|------|------|------|
| **API响应时间** | ≤ 30秒 | 正常情况下10-30秒 |
| **图片上传** | ≤ 5秒 | 10M以内的图片 |
| **界面加载** | ≤ 1秒 | Tab切换、内容显示 |
| **操作反馈** | 即时 | 点击后立即显示反馈 |

---

## 📊 数据统计需求

### 6.1 统计维度

**使用数据：**
- 换背景调用次数
- 成功率/失败率
- 平均生成时间
- 背景生成 vs 背景上传使用比例

**用户行为：**
- 最常用的背景描述
- 预设示例点击率
- 重新生成频率
- 下载率

**质量指标：**
- 用户满意度（通过率）
- 负向反馈率
- 问题类型分布

---

### 6.2 日志记录

**前端日志：**
```javascript
console.log('🎨 自定义换背景参数:', {
  productImageUrl,
  backgroundDescription,
  prompt,
  bgRatio,
  bgMethod
});
console.log('📤 发送换背景请求:', apiPayload);
console.log('📥 API返回结果:', result);
```

**后端日志：**
```python
print(f"==================================================")
print(f"🎨 换背景请求")
print(f"==================================================")
print(f"📷 图片URL: {image_url}")
print(f"📷 图片列表: {image_urls}")
print(f"📝 Prompt: {prompt}")
print(f"🤖 模型: {model}")
print(f"📐 背景比例: {bg_ratio}")
print(f"🎯 背景方法: {bg_method}")
```

---

## 🔒 安全需求

### 7.1 API密钥安全

**要求：**
- ✅ API密钥存储在后端
- ✅ 前端不暴露API密钥
- ✅ 使用环境变量管理
- ✅ 定期更换密钥

**实现：**
```python
# 后端配置
API_KEY = os.environ.get('NANOBANANA_API_KEY', 'sk-xxx')
```

---

### 7.2 文件上传安全

**验证措施：**
- ✅ 文件类型白名单（JPEG、PNG、WEBP）
- ✅ 文件大小限制（≤10M）
- ✅ 文件名消毒（防止路径遍历）
- ✅ 唯一文件名（防止覆盖）

**实现：**
```python
# 文件类型验证
validTypes = ['image/jpeg', 'image/jpg', 'image/png', 'image/webp']

# 文件大小验证
if file.size > 10 * 1024 * 1024:
    return error("文件大小不能超过 10M")

# 生成唯一文件名
filename = f"{timestamp}_{uuid}_{sanitized_name}"
```

---

### 7.3 内容安全

**图片审核：**
- 建议：集成内容审核API
- 检测违规内容
- 阻止敏感图片生成

**Prompt过滤：**
- 建议：过滤敏感词
- 限制Prompt长度（≤500字符）
- 记录用户输入用于审计

---

## 📱 兼容性需求

### 8.1 浏览器兼容性

**支持的浏览器：**
- ✅ Chrome 90+
- ✅ Safari 14+
- ✅ Firefox 88+
- ✅ Edge 90+

**不支持：**
- ❌ IE 11及以下

**关键技术：**
- Fetch API
- Async/Await
- ES6+ 语法
- CSS Grid

---

### 8.2 设备兼容性

**支持设备：**
- ✅ 桌面电脑（推荐）
- ✅ 平板电脑
- ⚠️ 手机（布局适配有限）

**屏幕分辨率：**
- 推荐：≥ 1366x768
- 最小：1024x768

---

## 🚀 部署需求

### 9.1 环境要求

**后端环境：**
- Python 3.9+
- Flask 3.0+
- flask-cors 4.0+
- requests 2.31+

**第三方服务：**
- NanoBanana AI API
- 腾讯云COS（可选，用于图片存储）

---

### 9.2 配置项

**环境变量：**
```bash
# NanoBanana API密钥
NANOBANANA_API_KEY=sk-xxxx

# COS配置（可选）
COS_SECRET_ID=xxx
COS_SECRET_KEY=xxx
COS_BUCKET=xxx
COS_REGION=xxx
```

**配置文件：**
```python
# API基础URL
API_BASE_URL = "https://grsai.dakka.com.cn"

# 上传文件夹
UPLOAD_FOLDER = "uploads"

# COS配置
COS_BUCKET = "12131231-1302391623"
COS_REGION = "ap-beijing"
COS_FOLDER = "nanobanana/web"
```

---

### 9.3 启动命令

**后端启动：**
```bash
cd /path/to/jarvis-admin-master
python3 api_server.py
```

**前端访问：**
```bash
open creative-tools-prototype.html
```

**健康检查：**
```bash
curl http://localhost:5000/api/health
```

---

## 📈 成功指标

### 10.1 功能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| **API成功率** | ≥ 95% | NanoBanana API调用成功率 |
| **生成成功率** | ≥ 90% | 最终生成图片成功率 |
| **平均响应时间** | ≤ 25秒 | 从提交到返回结果 |
| **用户满意度** | ≥ 80% | 通过率作为满意度指标 |

---

### 10.2 业务指标

| 指标 | 说明 |
|------|------|
| **日均使用次数** | 监控功能活跃度 |
| **用户留存率** | 重复使用率 |
| **功能偏好** | 背景生成 vs 背景上传比例 |
| **下载率** | 生成结果的实际使用率 |

---

## 🔄 迭代计划

### Phase 1：基础功能 ✅ 已完成

- ✅ 背景生成模式
- ✅ 背景上传模式
- ✅ 基础UI和交互
- ✅ NanoBanana API集成
- ✅ 错误处理

### Phase 2：功能增强（建议）

- [ ] 批量换背景（多张商品图）
- [ ] 背景图库（预设常用背景）
- [ ] 历史记录（保存上传的背景图）
- [ ] 对比预览（原图 vs 换背景后）

### Phase 3：智能优化（建议）

- [ ] 智能推荐背景（根据商品类别）
- [ ] 自动优化参数（根据商品特征）
- [ ] A/B测试（多版本生成）
- [ ] 质量评分（AI评估生成质量）

---

## 📝 附录

### A. Prompt模板库

**背景生成类：**
1. 户外场景：`清新自然的户外场景，蓝天白云，阳光明媚，绿色草地`
2. 室内场景：`现代简约室内背景，白色墙面，明亮光线，干净整洁`
3. 渐变色彩：`渐变色彩背景，从浅蓝到深蓝，柔和过渡，简约时尚`
4. 纯色背景：`纯色背景，高级灰色调，柔和光影，商务风格`

**背景上传类：**
- 固定Prompt：`请将第一张图的背景替换为第二张图`

---

### B. 常见问题（FAQ）

**Q1: 为什么需要选择背景比例？**

A: 背景比例决定最终生成图片的尺寸，不同的投放渠道需要不同的比例：
- 1:1 - 方图，适合大部分平台
- 3:4 - 竖图，适合移动端
- 9:16 - 超竖图，适合短视频平台

**Q2: 背景生成和背景上传的区别？**

A: 
- **背景生成**：AI根据文字描述创造背景，适合需要创意的场景
- **背景上传**：使用真实拍摄的背景图，适合需要精确控制的场景

**Q3: 生成时间为什么这么长？**

A: NanoBanana API需要使用AI模型生成图片，通常需要10-30秒。复杂场景可能更久。

**Q4: 如果生成结果不满意怎么办？**

A: 可以：
1. 点击"重新生成"再试一次
2. 修改背景描述或更换背景图
3. 使用"NB修复"功能进一步优化（图片创作功能）

---

### C. API速率限制

**NanoBanana API限制：**
- 具体限制请参考NanoBanana API文档
- 建议：控制并发请求数
- 建议：添加请求队列机制

**建议的限流策略：**
- 同一用户：3次/分钟
- 系统总计：10次/分钟
- 超限提示："请求过于频繁，请稍后重试"

---

### D. 成本估算

**NanoBanana API调用成本：**
- 每次换背景：1次API调用
- 每次调用成本：（根据实际API定价）
- 月均成本估算：调用次数 × 单次成本

**腾讯云COS成本：**
- 存储成本：（根据存储量）
- 流量成本：（根据下载量）
- 建议：定期清理过期文件

---

### E. 监控告警

**需要监控的指标：**
1. API调用成功率（低于90%告警）
2. 平均响应时间（超过40秒告警）
3. 服务器CPU/内存使用率
4. 存储空间使用率
5. API密钥有效性

**告警渠道：**
- 邮件通知
- 企业微信/钉钉
- 监控平台（如Grafana）

---

## 🎉 总结

### 功能完成度

- ✅ **核心功能：** 100%完成
- ✅ **UI界面：** 100%完成
- ✅ **API集成：** 100%完成
- ✅ **错误处理：** 100%完成
- ✅ **文档：** 100%完成

### 技术亮点

1. **双模式设计** - 背景生成 + 背景上传，灵活满足不同需求
2. **智能Prompt** - 自动构建Prompt，用户无需了解AI指令
3. **渐进式增强** - 从背景图库到AI生成，功能层次清晰
4. **完善体验** - 实时反馈、错误处理、可重复操作

### 产品价值

- 💼 **业务价值** - 提升内容生产效率，降低人力成本
- 🎨 **创意价值** - AI赋能创意，突破传统限制
- 📈 **技术价值** - AI能力平台化，可复用可扩展

---

## 📞 联系方式

**技术支持：**
- 文档：`换背景功能使用说明.md`
- 文档：`背景上传功能使用说明.md`
- API文档：`NANOBANANA_API_INTEGRATION.md`

**相关文件：**
- 前端实现：`creative-tools-prototype.html`
- 后端实现：`api_server.py`
- 配置说明：`README.md`

---

**文档版本：** v1.0
**最后更新：** 2025-10-09
**状态：** ✅ 功能已实现并上线

