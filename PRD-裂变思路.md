# 裂变思路 — 简要 PRD

> 文档版本：v1.0  
> 所属模块：创意工具 / 万能指令 / 仿写裂变 Tab  
> 上级文档：[PRD-仿写裂变.md](./PRD-仿写裂变.md)  
> 接口文档：[外投图-仿写裂变接口文档.md](./外投图-仿写裂变接口文档.md)  
> 原型参考：`creative-tools-prototype.html`（万能指令 → 仿写裂变 → 裂变方向面板下方）

---

## 1. 功能概述

「裂变思路」为**独立选填字段**，与三种裂变方向（Auto / 实拍风 / 模版化）**分开配置**。用户可填写一条自然语言指引，作用于本次任务中**所有已选裂变方向**的结果组，牵引更细致的生成。

**业务价值：** 在固定风格模板之外，用统一话术补充 campaign 侧重点（如突出价格、功能、上身效果等），无需为每个方向重复输入。

---

## 2. 适用范围

| 项 | 说明 |
|----|------|
| 生效 Tab | 仅「仿写裂变」 |
| 生效复刻策略 | **仅裂变**、**仿写&裂变**（「仅仿写」不展示裂变方向及裂变思路） |
| 素材类型 | 单品单图(101)、多品多图(104) 均适用 |
| 与裂变方向关系 | **独立于** Auto / 实拍风 / 模版化步进器；填写后附加到各方向产出的每一组 |

---

## 3. UI 规格

### 3.1 布局位置

位于「裂变方向」灰色面板**下方**，为独立表单项：

```
┌─────────────────────────────────────────────────┐
│ 裂变方向 *                                       │
│ ┌─────────────────────────────────────────────┐ │
│ │ Auto / 实拍风 / 模版化（各含步进器）          │ │
│ │ 预计产出约 N 张（Auto + 实拍 + 模版）        │ │
│ └─────────────────────────────────────────────┘ │
│ 裂变思路 ⓘ                                       │
│ [──────── 单行文本输入框 ────────────────────] │
└─────────────────────────────────────────────────┘
```

### 3.2 控件说明

| 元素 | 规格 |
|------|------|
| 字段标题 | 「裂变思路」 |
| 说明 ⓘ | hover 展示：「选填，针对各裂变方向补充更细致的生成指引。指令遵循可能存在偏差」 |
| 文本输入框 | 单行；placeholder：`例如：突出价格优势、突出商品功能与上身效果` |
| 必填 | **否** |

### 3.3 三种裂变方向（对照）

| 方向 | 描述文案 | 默认数量 | 接口字段 |
|------|----------|----------|----------|
| Auto | 系统自动分配裂变风格 | 2 | **待确认** |
| 实拍风 | 生活化/主题化场景，氛围感强 | 0 | `sceneFissionNum` |
| 模版化 | 纯色/简洁色块背景，突出商品 | 0 | `solidFissionNum` |

---

## 4. 交互与业务规则

### 4.1 文案处理

| 原则 | 说明 |
|------|------|
| **整条传递** | 输入内容 trim 后原样作为 `fissionThinkingText`，不因标点拆分为多条 |
| **全局附加** | 同一任务内，Auto / 实拍风 / 模版化产出的**每一组**均携带相同裂变思路 |
| **空值** | 未填写时不传或传空字符串，不影响方向数量校验 |

### 4.2 校验

| 规则 | 说明 |
|------|------|
| 裂变方向总量 | `Auto + 实拍风 + 模版化 ≥ 1`（与裂变思路是否填写无关） |
| 裂变思路 | **选填**；建议正式版上限 **200 字**（待确认） |

---

## 5. 前端数据结构（原型）

### 5.1 表单 `collectFissionFormData()`

```ts
{
  autoFissionNum: number;
  sceneFissionNum: number;   // UI：实拍风
  solidFissionNum: number;   // UI：模版化
  fissionThinkingText: string; // 选填，trim 后原样传递
}
```

### 5.2 结果组展开 `collectFissionGroupSpecs()`

每组 spec 附加：

```ts
{
  type: 'single' | 'grid',
  label: string,              // 如「实拍风 1」「模版化」
  seed: string,
  fissionThinking: string     // 与 fissionThinkingText 相同
}
```

### 5.3 任务历史回溯

| 字段 | 说明 |
|------|------|
| `fissionThinkingText` | 裂变思路文案 |
| `customDirectionText` | **兼容旧数据**，回填时映射至裂变思路输入框 |

---

## 6. 接口映射（待后端确认 ⚠️）

**建议：**

```json
{
  "fissionThinkingPrompt": "突出价格优势、突出商品功能与上身效果"
}
```

- 单字符串，任务级；作用于本次请求所有裂变方向组。
- 与 `sceneFissionNum` / `solidFissionNum` / Auto 数量字段并列提交。

---

## 7. 研发验收清单

- [ ] 裂变思路独立于三种裂变方向，非面板内第 4 行
- [ ] 选填；未填不影响提交
- [ ] 各方向每组结果携带相同 `fissionThinking`
- [ ] 仅仿写模式下隐藏裂变思路区域
- [ ] 任务历史回填 `fissionThinkingText`；兼容 `customDirectionText`
- [ ] 接口字段 `fissionThinkingPrompt`（或等价命名）对接

---

## 8. 变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-05-19 | 自「自定义裂变方向」重构为独立「裂变思路」；方向收敛为 Auto / 实拍风 / 模版化 |
