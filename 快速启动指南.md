# 🚀 AI创意工具 - 快速启动指南

## ⚡ 最快启动方式

```bash
cd /Users/lucien.xu/Desktop/代码汇总/jarvis-admin-master
./open-preview.sh
```

**这个脚本会：**
- ✅ 自动清除浏览器缓存
- ✅ 添加时间戳防止缓存
- ✅ 打开最新版本的页面

---

## 📋 完整启动步骤

### 1. 启动后端API服务器（如果需要）

```bash
cd /Users/lucien.xu/Desktop/代码汇总/jarvis-admin-master
python3 api_server.py &
```

**检查服务器状态：**
```bash
curl http://localhost:5000/api/health
```

**预期输出：**
```json
{
  "service": "jarvis-admin-api",
  "status": "healthy",
  "nanobanana_configured": true
}
```

---

### 2. 打开前端页面

**方法1: 使用启动脚本（推荐）**
```bash
./open-preview.sh
```

**方法2: 手动打开**
```bash
open creative-tools-prototype.html
```

然后按 `Cmd + Shift + R` 强制刷新。

---

## 🔧 常见问题快速解决

### 问题1: 看到旧版本的页面

**解决方案：**
```bash
# 运行启动脚本
./open-preview.sh

# 或者在浏览器中按
Cmd + Shift + R  # 强制刷新
```

---

### 问题2: 后端API连接失败

**现象：**
```
net::ERR_CONNECTION_REFUSED
```

**解决方案：**
```bash
# 启动后端服务器
cd /Users/lucien.xu/Desktop/代码汇总/jarvis-admin-master
python3 api_server.py
```

---

### 问题3: 启动脚本无法执行

**现象：**
```
Permission denied
```

**解决方案：**
```bash
# 添加执行权限
chmod +x open-preview.sh

# 然后再次运行
./open-preview.sh
```

---

## 📍 版本确认

打开页面后，查看**右下角的版本号**：

```
v2025.10.09-20:47
```

**功能：**
- 鼠标悬停：显示详细信息（版本、加载时间、缓存状态）
- 点击：复制版本信息到剪贴板
- 控制台：自动输出版本信息（按F12查看）

---

## 🎯 开发工作流

### 标准流程

1. **启动后端**（只需一次）
   ```bash
   python3 api_server.py &
   ```

2. **修改代码**
   - 编辑 `creative-tools-prototype.html`
   - 或编辑 `api_server.py`

3. **预览修改**
   ```bash
   ./open-preview.sh
   ```

4. **验证功能**
   - 测试新功能
   - 检查控制台日志
   - 确认版本号正确

5. **重复2-4步**

---

## 📊 关键功能模块

| 功能 | 状态 | 备注 |
|------|------|------|
| **扩图** | ✅ 就绪 | 支持NB模型、aspectRatio参数 |
| **换背景** | ✅ 就绪 | 支持比例控制、背景上传 |
| **图创工具** | ✅ 就绪 | 支持NB修复功能 |
| **版本显示** | ✅ 已添加 | 右下角版本号 |
| **缓存控制** | ✅ 已优化 | Meta标签 + 启动脚本 |

---

## 🔑 重要路径

```
项目根目录/
├── creative-tools-prototype.html    # 主前端页面
├── api_server.py                     # 后端API服务器
├── open-preview.sh                   # 启动脚本 ⭐
├── 浏览器缓存问题解决方案.md        # 详细文档
└── 快速启动指南.md                  # 本文档
```

---

## 💡 实用快捷键

| 操作 | Mac快捷键 | 说明 |
|------|-----------|------|
| **强制刷新** | `Cmd + Shift + R` | 忽略缓存重新加载 |
| **清除缓存** | `Cmd + Shift + Delete` | 打开清除窗口 |
| **开发者工具** | `Cmd + Option + I` | 查看控制台 |
| **隐私浏览** | `Cmd + Shift + N` | 无缓存测试 |
| **查看源代码** | `Cmd + Option + U` | 查看HTML源码 |

---

## 🎨 版本历史

| 版本 | 日期 | 主要变更 |
|------|------|----------|
| v2025.10.09-20:47 | 2025-10-09 | 添加缓存控制、版本显示、启动脚本 |
| v2025.10.09-20:00 | 2025-10-09 | 优化Prompt规则、添加aspectRatio |
| v2025.10.09-19:00 | 2025-10-09 | 换背景功能比例参数验证 |

---

## 📞 快速命令参考

```bash
# 完整启动（后端 + 前端）
python3 api_server.py &
./open-preview.sh

# 仅启动前端
./open-preview.sh

# 检查后端健康
curl http://localhost:5000/api/health

# 停止后端
lsof -ti:5000 | xargs kill -9

# 查看后端日志
tail -f nohup.out  # 如果使用nohup启动

# 查看文件修改时间
ls -lh creative-tools-prototype.html

# 清除所有缓存
rm -rf ~/Library/Caches/Google/Chrome/*
rm -rf ~/Library/Caches/com.apple.Safari/*
```

---

## 🎉 完成检查清单

启动前检查：
- [ ] 后端服务器运行在 http://localhost:5000
- [ ] 启动脚本有执行权限（`chmod +x open-preview.sh`）
- [ ] NanoBanana API密钥已配置

打开页面后检查：
- [ ] 右下角显示版本号 `v2025.10.09-20:47`
- [ ] 控制台显示版本信息（按F12）
- [ ] 页面功能正常（扩图、换背景、图创）

---

## 🔗 相关文档

- 📄 [浏览器缓存问题解决方案.md](浏览器缓存问题解决方案.md) - 详细的缓存处理文档
- 📄 [换背景功能-比例参数验证报告.md](换背景功能-比例参数验证报告.md) - 比例参数验证
- 📄 [扩图Prompt规则-最终版.md](扩图Prompt规则-最终版.md) - Prompt构建规则
- 📄 [aspectRatio参数优化说明.md](aspectRatio参数优化说明.md) - API参数说明

---

## ✨ 提示

> **每次修改代码后，都运行 `./open-preview.sh` 确保看到最新版本！**

---

**更新时间：** 2025-10-09 20:50
**当前版本：** v2025.10.09-20:47
**状态：** ✅ 生产就绪

