# 万能指令 · 快速启动

## 仅预览 UI

```bash
cd "$(dirname "$0")"
python3 -m http.server 8877
```

浏览器打开：http://localhost:8877/creative-tools-prototype.html

## 指令生成（需后端）

```bash
python3 api_server.py
```

另开终端启动静态服务，在「指令生成」Tab 上传图片并提交。

健康检查：`curl http://localhost:5000/api/health`

## 仿写裂变

原型内为演示模式（mock 结果）。正式对接见 `外投图-仿写裂变接口文档.md`。

## 在线 Demo

https://xuyan14.github.io/ai-admin-demo/creative-tools-prototype.html
