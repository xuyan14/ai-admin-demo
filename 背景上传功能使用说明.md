# 自定义背景图 - 背景上传功能使用说明

## ✅ 功能已实现

在"自定义背景图"模式下，现在提供**两种方式**来替换商品背景：

1. **背景生成**（默认）- 通过文字描述生成背景
2. **背景上传**（新增）- 上传真实背景图替换商品背景

---

## 🎯 功能概述

### **背景生成模式**
- 用户输入背景描述（Prompt）
- AI根据描述生成背景并替换商品背景
- Prompt格式：`请帮我替换图片背景。背景为{用户描述}`

### **背景上传模式** ⭐ 新增
- 用户上传一张背景图
- AI将商品图的背景替换为上传的背景图
- Prompt格式：`请将第一张图的背景替换为第二张图`
- 图片传递：`[商品图URL, 背景图URL]`

---

## 📱 使用步骤

### 模式1：背景生成（原有功能）

1. 进入"换背景"功能
2. 选择商品图片
3. 选择背景比例
4. 点击"自定义背景图"标签
5. **确保"背景生成"tab被选中**（默认选中）
6. 输入背景描述，如："蓝天白云的户外场景"
7. 点击"生产"按钮
8. 等待生成结果

---

### 模式2：背景上传（新增功能）⭐

1. 进入"换背景"功能
2. 选择商品图片
3. 选择背景比例
4. 点击"自定义背景图"标签
5. **点击"背景上传"tab**
6. 点击上传区域，选择背景图文件
   - 支持格式：JPEG/JPG/PNG/WEBP
   - 大小限制：10M以内
7. 等待背景图上传完成（显示预览）
8. 点击"生产"按钮
9. 等待生成结果

---

## 🔧 技术实现

### 前端参数

#### 背景生成模式
```javascript
{
  image_url: "https://...商品图URL",
  image_urls: ["https://...商品图URL"],
  prompt: "请帮我替换图片背景。背景为蓝天白云的户外场景",
  model: "nb",
  function: "change-background",
  bg_ratio: "1:1",
  bg_method: "generate"
}
```

#### 背景上传模式
```javascript
{
  image_url: "https://...商品图URL",
  image_urls: [
    "https://...商品图URL",
    "https://...背景图URL"
  ],
  prompt: "请将第一张图的背景替换为第二张图",
  model: "nb",
  function: "change-background",
  bg_ratio: "1:1",
  bg_method: "upload"
}
```

### 后端API变更

**文件：** `api_server.py`

**新增参数支持：**
```python
# 支持多图片
image_urls = data.get('image_urls', [image_url] if image_url else [])
bg_method = data.get('bg_method', 'generate')  # 背景生成或背景上传

# 调用NanoBanana API
result = nanobanana_client.draw_image(
    prompt=prompt,
    image_urls=image_urls,  # 传递图片列表
    model="nano-banana-fast"
)
```

---

## 📊 界面展示

### 二级Tab切换
```
┌─────────────────────────────────┐
│ [选择背景图] [自定义背景图]      │
├─────────────────────────────────┤
│                                 │
│  ┌──────────┬──────────┐        │
│  │背景生成(✓)│  背景上传 │        │  ← 二级Tab
│  └──────────┴──────────┘        │
│                                 │
│  背景描述输入框...              │
│  [户外场景] [室内场景] ...      │
│                                 │
└─────────────────────────────────┘
```

### 背景上传界面
```
┌─────────────────────────────────┐
│  背景生成  │ [背景上传(✓)]       │
├─────────────────────────────────┤
│                                 │
│  上传背景图                     │
│  ┌─────────────────────────┐   │
│  │       ☁️                 │   │
│  │   点击上传背景图          │   │
│  │ JPEG/JPG/PNG/WEBP 10M内 │   │
│  └─────────────────────────┘   │
│                                 │
│  ℹ️ 提示：上传的背景图将替换   │
│     商品图的背景                │
│                                 │
└─────────────────────────────────┘
```

---

## 🎨 新增JavaScript函数

### 1. `switchBgMethod(method)`
切换二级tab（背景生成/背景上传）

**参数：**
- `method`: `'generate'` 或 `'upload'`

**功能：**
- 更新tab按钮状态
- 切换内容显示

---

### 2. `triggerBgUpload()`
触发背景图文件选择

**功能：**
- 点击隐藏的文件输入框

---

### 3. `handleBgFileUpload(event)`
处理背景图上传

**功能：**
- 验证文件类型和大小
- 显示本地预览
- 上传到服务器（`/api/upload-image`）
- 存储背景图URL到 `uploadedBgImageUrl`

---

### 4. `clearBgUpload()`
清除上传的背景图

**功能：**
- 清空 `uploadedBgImageUrl`
- 重置UI显示

---

### 5. `submitCustomChangeBg()`（修改）
根据模式提交不同的请求

**逻辑：**
```javascript
if (currentBgMethod === 'generate') {
    // 背景生成：单图 + 描述prompt
    prompt = `请帮我替换图片背景。背景为${bgDescription}`;
    imageUrls = [productImageUrl];
} else if (currentBgMethod === 'upload') {
    // 背景上传：双图 + 替换prompt
    prompt = '请将第一张图的背景替换为第二张图';
    imageUrls = [productImageUrl, uploadedBgImageUrl];
}
```

---

## 🧪 测试步骤

### 测试背景上传模式

1. **启动后端服务器**
   ```bash
   python3 api_server.py
   ```

2. **打开前端页面**
   ```bash
   open creative-tools-prototype.html
   ```

3. **操作步骤**
   - 进入"换背景"功能
   - 输入商品ID并查询
   - 选择一张商品图
   - 选择背景比例（如 1:1）
   - 点击"自定义背景图"标签
   - **点击"背景上传"tab**
   - 点击上传区域
   - 选择一张背景图（如：室内、户外场景图）
   - 等待上传完成（显示"✅ 背景图上传成功！"）
   - 点击"生产"按钮
   - 观察加载状态（显示"使用上传的背景图"）
   - 查看生成结果

4. **预期行为**
   - ✅ 背景图成功上传到服务器
   - ✅ 显示背景图预览
   - ✅ 提示"正在使用NB模型生成换背景..."
   - ✅ 显示Prompt："请将第一张图的背景替换为第二张图"
   - ✅ 显示"图片数量：2张（商品图+背景图）"
   - ✅ 后端日志显示传递2张图片
   - ✅ 生成的结果图显示商品背景被替换

---

## 📝 完整文件清单

### 前端修改
**文件：** `creative-tools-prototype.html`

**修改内容：**
1. HTML结构
   - 添加二级tab切换UI
   - 添加背景上传区域
   - 添加背景图预览

2. JavaScript函数
   - `switchBgMethod()` - 切换tab
   - `triggerBgUpload()` - 触发上传
   - `handleBgFileUpload()` - 处理上传
   - `clearBgUpload()` - 清除上传
   - `submitCustomChangeBg()` - 支持两种模式

3. 全局变量
   - `currentBgMethod` - 当前选择的背景方法
   - `uploadedBgImageUrl` - 上传的背景图URL

### 后端修改
**文件：** `api_server.py`

**修改内容：**
- 支持 `image_urls` 参数（图片列表）
- 支持 `bg_method` 参数（背景方法标识）
- 传递完整图片列表给NanoBanana API

---

## 🎯 对比总结

| 特性 | 背景生成 | 背景上传 |
|------|----------|----------|
| **用户输入** | 文字描述 | 上传图片 |
| **图片数量** | 1张（商品图） | 2张（商品图+背景图） |
| **Prompt** | 描述性（如"蓝天白云..."） | 固定（"将第一张图的背景替换为第二张图"） |
| **适用场景** | 需要AI创造性生成背景 | 有现成背景图需要替换 |
| **优点** | 灵活、创意无限 | 精确、可控性强 |

---

## 🚀 下一步建议

1. **批量处理**
   - 支持同时上传多张商品图
   - 使用同一张背景图批量替换

2. **背景图库**
   - 预设常用背景图
   - 用户可从库中选择或上传自定义

3. **预览对比**
   - 显示原图 vs 换背景后的对比
   - A/B测试功能

4. **历史记录**
   - 保存用户上传的背景图
   - 快速重用历史背景

---

## 📞 测试确认

页面已打开！现在可以测试：

### 快速测试清单

- [ ] 进入换背景功能
- [ ] 点击"自定义背景图"
- [ ] 切换到"背景上传"tab
- [ ] 上传一张背景图
- [ ] 观察上传进度和预览
- [ ] 点击"生产"按钮
- [ ] 查看加载状态（显示使用上传的背景图）
- [ ] 查看后端日志（显示2张图片）
- [ ] 查看生成结果

---

## ✨ 功能完成

🎉 **自定义背景图 - 背景上传功能已完整实现！**

- ✅ 二级tab切换UI
- ✅ 背景图上传功能
- ✅ 图片预览
- ✅ 双图模式API调用
- ✅ 后端多图支持
- ✅ 错误处理
- ✅ 用户体验优化

享受新功能吧！🚀

