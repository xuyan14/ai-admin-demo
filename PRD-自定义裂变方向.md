# 自定义裂变方向 — 简要 PRD

> 文档版本：v1.1  
> 所属模块：创意工具 / 万能指令 / 仿写裂变 Tab → 裂变方向（FF8）  
> 上级文档：[PRD-仿写裂变.md](./PRD-仿写裂变.md)  
> 接口文档：[外投图-仿写裂变接口文档.md](./外投图-仿写裂变接口文档.md)  
> 原型参考：`creative-tools-prototype.html`（万能指令 → 仿写裂变 → 裂变方向面板第 4 行）

---

## 1. 功能概述

在现有裂变方向（Auto / 场景风 / 实拍风）基础上，新增第 **4 种裂变方式：自定义裂变方向**。用户通过**步进器设置生成数量**，并通过**文本输入框**填写**一条完整的裂变指令**（整体作为单次仿写/裂变诉求，**不按标点拆分为多条**），系统按该指令生成 N 组结果。

**业务价值：** 在固定风格模板之外，支持运营用自然语言描述 campaign 侧重点，一次输入、多组产出。

---

## 2. 适用范围

| 项 | 说明 |
|----|------|
| 生效 Tab | 仅「仿写裂变」 |
| 生效复刻策略 | **仅裂变**、**仿写&裂变**（「仅仿写」不展示裂变方向整块） |
| 素材类型 | 单品单图(101)、多品多图(104) 均适用 |
| 与预设方向关系 | 与 Auto / 场景风 / 实拍风**并列、可叠加**；各方向数量独立配置 |

---

## 3. UI 规格

### 3.1 布局位置

位于「裂变方向」面板内，作为**第 4 行**，与 Auto / 场景风 / 实拍风同一灰色面板：

```
┌─────────────────────────────────────────────────┐
│ [图标] Auto          系统自动分配…     [ − 2 + ] │
│ [图标] 场景风        生活化/主题化…    [ − 0 + ] │
│ [图标] 实拍风        真实场景/上身…    [ − 0 + ] │
│ [图标] 自定义裂变方向 ⓘ              [ − 0 + ] │
│ [──────── 文本输入框（整条裂变指令）──────────] │
│ 预计产出约 N 张（Auto + 场景 + 实拍 + 自定义）   │
└─────────────────────────────────────────────────┘
```

### 3.2 控件说明

| 元素 | 规格 |
|------|------|
| 行标题 | 「自定义裂变方向」 |
| 说明 ⓘ | hover 展示：「选填。输入框内容为整条裂变指令（不按标点拆分），步进器控制生成组数。指令遵循可能存在偏差」 |
| 数量步进器 | 0~10，默认 **0** |
| 文本输入框 | 单行；placeholder：`例如：突出价格优势、突出商品功能与上身效果`；**始终可编辑** |

### 3.3 默认值（裂变方向整块）

| 方向 | 默认数量 |
|------|----------|
| Auto | 2 |
| 场景风 | 0 |
| 实拍风 | 0 |
| **自定义** | **0** |

### 3.4 预计产出摘要

- **101：** `预计产出约 {total} 张（Auto {a} + 场景 {s} + 实拍 {p} + 自定义 {c}）`
- **104：** `预计产出 {total} 套九宫格（… + 自定义 {c}）`
- `total = auto + scene + solid + custom`

---

## 4. 交互与业务规则

### 4.1 整条指令，不拆分

| 原则 | 说明 |
|------|------|
| **不按标点解析** | 输入框内容原样作为**一条** `customDirectionText`，不因 `,，;；、换行` 等拆成多条指令 |
| **步进器定组数** | 步进器 = `N` 时产出 **N 个结果组**，每组携带**相同**裂变指令 |
| **示例** | 输入「突出价格优势、突出商品功能」且 N=2 → 两组均使用整条文案作为指令，而非拆成两条 |

### 4.2 组标题（展示用）

组标题仅用于 UI 区分多组结果，**不代表不同指令**：

| 场景 | `generationRecord.groups[].label` |
|------|-----------------------------------|
| N=1，有文案 | 与输入文案相同 |
| N>1，有文案 | `{文案}（1）`、`{文案}（2）`… |
| N>0，无文案 | `自定义裂变` 或 `自定义裂变 1`…`N` |

每组 `customDirection` / 提交接口字段均传**完整** `customDirectionText`。

### 4.3 校验

| 规则 | 说明 |
|------|------|
| 裂变方向总量 | `Auto + 场景风 + 实拍风 + 自定义 ≥ 1` |
| 自定义文案 | **选填**；N>0 且文案为空时，按默认组标题生成 |
| 文案长度 | 正式版建议上限 **200 字**（待确认） |

---

## 5. 前端数据结构（原型）

### 5.1 表单 `collectFissionFormData()`

```ts
{
  autoFissionNum: number;
  sceneFissionNum: number;
  solidFissionNum: number;
  customFissionNum: number;     // 步进器：生成组数
  customDirectionText: string;  // 整条指令，trim 后原样传递，不拆分
}
```

### 5.2 结果组展开 `collectFissionGroupSpecs()`

按 `customFissionNum` 循环，每组：

```ts
{
  type: 'single' | 'grid',
  label: string,              // §4.2 展示标题
  seed: `custom-${i}`,
  customDirection: string     // 始终为完整 customDirectionText
}
```

### 5.3 任务历史回溯

| 字段 | 说明 |
|------|------|
| `fissionCustomNum` | 步进器值 |
| `customDirectionText` | 完整输入文案 |

---

## 6. 接口映射（待后端确认 ⚠️）

**建议：**

```json
{
  "customFissionNum": 2,
  "customFissionPrompt": "突出价格优势、突出商品功能与上身效果"
}
```

- `customFissionPrompt`：**单字符串**，不按组拆列表。
- 服务端按 `customFissionNum` 生成 N 组结果，每组应用**同一条** prompt。
- 若需区分组序号，由服务端在内部处理，前端不传 `promptList[]`。

---

## 7. 生成记录与任务展示

- N 组自定义结果，组间指令相同，标题用序号区分（§4.2）。
- 支持单组失败重试；104 每组为 1 套九宫格。

---

## 8. 研发验收清单

- [ ] 输入框内容**不**按标点拆分为多条指令
- [ ] N 组结果共用同一条 `customDirectionText`
- [ ] 步进器控制组数；摘要 total 含自定义
- [ ] info 悬停文案含「不按标点拆分」说明
- [ ] 任务历史回填 `fissionCustomNum` + `customDirectionText`
- [ ] 接口按 `customFissionNum` + `customFissionPrompt`（单字符串）对接

---

## 9. 变更记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-29 | 初版 |
| v1.1 | 2026-06-29 | 输入改为整条指令，不按标点拆分；步进器仅控制组数 |
