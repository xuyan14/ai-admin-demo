# 自定义换背景功能使用说明

## ✅ 功能已实现并测试通过

### 📋 功能概述

自定义换背景功能允许用户通过输入背景描述（Prompt）来为商品图生成新的背景，调用NanoBanana API实现智能换背景。

---

## 🚀 使用步骤

### 1. 启动后端API服务器

```bash
cd /Users/lucien.xu/Desktop/代码汇总/jarvis-admin-master
python3 api_server.py
```

**确认服务器启动成功：**
- 看到 `Running on http://127.0.0.1:5000` 
- 可用接口中包含 `POST /api/change-background  - 换背景接口`

---

### 2. 打开前端页面

```bash
open creative-tools-prototype.html
```

---

### 3. 操作流程

#### 步骤1：进入换背景功能
- 点击左侧导航栏的 **"换背景"**

#### 步骤2：选择商品图片
- 在"商品图片"区域选择一张商品图
- 确保图片被选中（显示蓝色✓标记）

#### 步骤3：选择背景图比例
- 选择期望的背景图比例（如：1:1、3:4等）

#### 步骤4：切换到自定义背景图模式
- 点击 **"自定义背景图"** 标签页
- 界面会切换到自定义背景描述输入区域

#### 步骤5：输入背景描述
- 在"背景描述 (Prompt)"文本框中输入您想要的背景描述
- 例如：
  - "蓝天白云的户外场景，阳光明媚，清新自然"
  - "现代简约室内背景，白色墙面，明亮光线，干净整洁"
  - "渐变色彩背景，从浅蓝到深蓝，柔和过渡，简约时尚"

**提示词示例按钮：**
- 点击预设的示例按钮可以快速填充常用背景描述

#### 步骤6：提交生成
- 点击右下角的 **"提交"** 按钮
- 系统会显示加载状态和调用的Prompt信息

#### 步骤7：查看结果
- 生成完成后，结果会显示在右侧"生成记录"面板
- 可以：
  - 在新窗口打开查看大图
  - 下载生成的图片
  - 重新生成

---

## 🔧 技术实现

### API调用流程

```
前端 → 后端API → NanoBanana API → 返回结果
```

### Prompt格式

```
请帮我替换图片背景。背景为{用户输入的背景描述}
```

### 请求参数

```json
{
  "image_url": "商品图片的URL",
  "prompt": "请帮我替换图片背景。背景为蓝天白云的户外场景",
  "model": "nb",
  "function": "change-background",
  "bg_ratio": "1:1"
}
```

### API端点

```
POST http://localhost:5000/api/change-background
```

---

## 📊 测试结果

### 测试命令

```bash
python3 test_change_background.py
```

### 测试结果示例

```
✅ API调用成功!
{
  "data": {
    "output_url": "https://file3.grsai.com/file/1c5263292c004648b685abca7ed9244a.png",
    "prompt": "请帮我替换图片背景。背景为蓝天白云的户外场景，阳光明媚，清新自然",
    "task_id": "f8d5779d-2f51-4169-8231-3dcb6327182f"
  },
  "status": "succeeded",
  "success": true
}

🎉 换背景生成成功!
🖼️  输出图片URL: https://file3.grsai.com/file/1c5263292c004648b685abca7ed9244a.png
```

---

## 🎯 功能特点

### ✨ 用户体验优化

1. **智能模式检测**
   - 自动识别用户选择的模式（背景图库 vs 自定义背景图）
   - 根据模式调用不同的处理逻辑

2. **实时反馈**
   - 显示加载状态
   - 显示实际调用的Prompt
   - Toast提示消息

3. **错误处理**
   - 参数验证（商品图、背景描述、比例）
   - API错误捕获和友好提示
   - 重试机制

4. **结果展示**
   - 大图预览
   - 一键下载
   - 显示生成时间和Prompt

### 🔄 异步任务支持

- 支持轮询任务状态（如果API返回task_id）
- 自动等待任务完成
- 超时保护（最多30次，每次2秒）

---

## 📝 代码文件

### 前端代码

**文件：** `creative-tools-prototype.html`

**关键函数：**
- `submitChangeBg()` - 主提交函数，检测模式
- `submitCustomChangeBg()` - 自定义背景图处理
- `showChangeBgResultWithRealImage()` - 结果展示
- `pollChangeBgTaskStatus()` - 任务状态轮询

### 后端代码

**文件：** `api_server.py`

**新增接口：**
```python
@app.route('/api/change-background', methods=['POST'])
def change_background():
    """换背景接口"""
```

### 测试代码

**文件：** `test_change_background.py`

**功能：** 自动化测试换背景API

---

## 🔍 调试信息

### 浏览器控制台日志

```javascript
🎨 自定义换背景参数: {
  imageUrl: "https://...",
  prompt: "请帮我替换图片背景。背景为...",
  bgRatio: "1:1"
}

📤 发送换背景请求: {...}
📥 API返回结果: {...}
```

### 后端服务器日志

```
==================================================
🎨 换背景请求
==================================================
📷 图片URL: https://...
📝 Prompt: 请帮我替换图片背景。背景为...
🤖 模型: nb
📐 背景比例: 1:1

🚀 开始调用NanoBanana API进行换背景...
📥 API原始返回: {...}
  ✅ 提取的output_url: https://...
  ✅ 提取的task_id: ...
  ✅ 提取的status: succeeded
```

---

## 🎓 常见问题

### Q1: 提交后没有反应？
**A:** 检查：
1. 后端API服务器是否启动
2. 浏览器控制台是否有错误
3. 是否选择了商品图片
4. 是否输入了背景描述

### Q2: 生成失败怎么办？
**A:** 
1. 查看错误提示信息
2. 检查API密钥是否正确
3. 查看后端服务器日志
4. 尝试使用简单的背景描述

### Q3: 生成时间太长？
**A:**
- NanoBanana API生成图片需要时间
- 通常在10-30秒内完成
- 如果超时，会显示错误提示

---

## 📞 技术支持

### 相关文件
- 前端实现：`creative-tools-prototype.html`
- 后端实现：`api_server.py`
- 测试脚本：`test_change_background.py`
- API文档：`NANOBANANA_API_INTEGRATION.md`

### 开发者信息
- 功能：自定义换背景（基于NanoBanana API）
- 状态：✅ 已实现并测试通过
- 测试日期：2025-10-09

---

## 🎉 总结

自定义换背景功能已完整实现并通过测试！

**主要成就：**
1. ✅ 前端界面完整（表单、输入、反馈）
2. ✅ 后端API接口完整（参数处理、API调用）
3. ✅ 错误处理完善（验证、捕获、提示）
4. ✅ 用户体验优化（加载状态、结果展示）
5. ✅ 自动化测试通过

**下一步建议：**
- 添加更多预设背景描述
- 支持批量换背景
- 添加历史记录功能
- 优化图片预览交互

