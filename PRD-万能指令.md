# 万能指令 PRD

> 文档版本：v1.0  
> 模块定位：创意工具 / 万能指令  
> 文档说明：仅描述功能点与功能逻辑，不涉及视觉与交互细节

---

## 1. 模块概述

「万能指令」是创意工具下的统一图像生成入口，用户上传图片素材并输入自然语言指令（Prompt），由后端调用 NanoBanana 系列 AI 绘画模型生成新图像。模块同时承担两类核心场景：

1. **指令生成（generate）**：基于自定义 Prompt + 上传图，按指定比例与数量生成新图。
2. **仿写裂变（fission）**：基于唯品会营销素材（含商品 ID），自动产出仿写或裂变变体。

模块以单页三栏布局承载：左侧为输入面板，中间为生成结果展示区，右侧为历史任务列表。

---

## 2. 功能点总览

| 编号 | 功能点 | 是否必填 | 说明 |
|------|--------|----------|------|
| F1 | 功能切换 Tab | - | 指令生成 / 仿写裂变 二选一 |
| F2 | 图片上传 | 是 | 多图上传，支持点击/拖拽/粘贴 |
| F3 | 上传图片排序与删除 | - | 拖拽 + 左右按钮调整图片顺序，单张删除，全部清空 |
| F4 | 模型选择（仅指令生成） | 是 | nano-banana-fast / nano-banana-pro / image-2 |
| F5 | Prompt 输入 | 是（指令生成） | 文本域，最大 2000 字，实时字符计数 |
| F6 | Prompt 管理器 | - | 卡片式 Prompt 库，支持新增/编辑/删除/筛选/应用 |
| F7 | 背景图比例 | 是（指令生成） | Auto / 1:1 / 3:4 / 9:16 / 16:9 |
| F8 | 生成数量 | 是 | 1 / 2 / 3 / 4 张 |
| F9 | 商品 ID（仅仿写裂变） | 条件必填 | 仿写、仿写&裂变模式必填 |
| F10 | 仿写裂变模式 | 是（仿写裂变） | 仿写&裂变 / 仿写 / 裂变 |
| F11 | 提交生成 | - | 校验 → 调后端 API → 中间面板展示加载/结果 |
| F12 | 任务历史与回溯 | - | 右侧任务列表展示历史任务，点击卡片回填全部配置 |
| F13 | 重新生成 | - | 结果页可一键再次生成 |

---

## 3. 详细功能逻辑

### 3.1 F1 - 功能切换 Tab

- 默认选中「指令生成」Tab。
- 切换到「仿写裂变」时：
  - 隐藏 `指令Prompt`、`模型选择`、`背景图比例` 表单区。
  - 显示 `商品ID`、`选择模式`、`生成数量` 区域。
  - 上传图片区不变，所有上传的图片在 Tab 之间共用同一份数组（不会清空）。
- 切换 Tab 不重置已上传图片，但会重置每个 Tab 内独立字段（如 Prompt 内容仅指令生成 Tab 持有）。

### 3.2 F2 - 图片上传

**接入方式：**
1. 点击「添加图片」按钮 → 弹出系统文件选择器。
2. 拖拽文件到上传区域。
3. `Ctrl+V` / `Cmd+V` 粘贴剪贴板中的图片。

**校验规则：**
- 格式限定：`image/jpeg`、`image/jpg`、`image/png`、`image/webp`。
- 单文件大小 ≤ 10 MB。
- 多文件并行校验，不通过的文件单独提示，通过的继续上传。

**上传流程：**
1. 前端构造 `FormData`，调用后端 `POST /api/upload-image`。
2. 后端将文件保存到本地临时目录，并尝试上传到腾讯云 COS：
   - 成功 → 返回云端 URL（`upload_type: cos`）。
   - 失败 → 降级返回本地 URL（`upload_type: local`）。
3. 前端将返回的 URL 追加到全局数组 `universalUploadedImages`，调用 `renderUniversalImages()` 重新渲染。
4. 失败的文件以 Toast 提示用户。

### 3.3 F3 - 上传图片排序与删除

**排序：**
- 每个图片缩略图均为 `draggable`，支持拖拽到目标位置后插入。
- 缩略图悬停显示底部工具条：左移（‹）、右移（›），首张禁用左移、末张禁用右移。
- 排序仅修改 `universalUploadedImages` 数组顺序，并触发整体重渲染。
- 数组顺序就是后续传给 NanoBanana API 的 `urls` 字段顺序，会影响生成结果。

**删除：**
- 单张：缩略图右上角的 `×` 按钮，确认后从数组中移除。
- 全部：「清空全部」按钮，需用户二次确认。
- 删除后自动重新编号 `1, 2, 3 ...`。

### 3.4 F4 - 模型选择

- 仅在「指令生成」Tab 出现。
- 单选项：`nano-banana-fast`（默认）、`nano-banana-pro`、`image-2`。
- 选中值直接作为 API 请求体中的 `model` 字段透传到后端。

### 3.5 F5 - Prompt 输入

- 多行文本域，最多 2000 字符。
- 输入时实时更新右下角 `已输入字数 / 2000` 计数。
- 提交校验：Prompt 不能为空，否则提示「请输入 Prompt」并禁止提交。

### 3.6 F6 - Prompt 管理器

> 入口：Prompt 输入框右上角「管理 Prompt」按钮。

#### 3.6.1 数据模型

每条 Prompt 包含以下字段：
- `id`：字符串，唯一标识。
- `name`：Prompt 名称。
- `content`：Prompt 正文。
- `category`：品类（服饰 / 美妆 / 鞋包 / 家居 / 数码 / 食品）。
- `style`：素材风格（质感简约风 / 原生实拍风 / 促销利益风 / 商品海报风 / 功能场景型）。
- `strategy`：内容策略（上身版型类 / 场景穿搭类 / 细节质感类 / 对比差异类 / 新客利益类 / 套装礼盒类 / 单品展示类 / 多品矩阵类 / IP联名类 / 场景展示类 / 持妆功效类 / 上脸/上手实拍类）。
- `status`：启用 / 停用。
- `previewImage`：预览图 URL（人工上传）。

数据持久化在浏览器 `localStorage`，键名 `universalPrompts`。首次加载时若为空，则注入 3 条默认 Prompt 数据。

#### 3.6.2 卡片网格视图

- 列表区为响应式卡片网格（`auto-fill, minmax(180px, 1fr)`）。
- 每张卡片自上而下展示：
  1. 正方形预览图（无图时显示占位图）。
  2. 状态徽章（启用 / 停用）。
  3. Prompt 名称（最多 2 行省略）。
  4. 三个分类 chip：品类（蓝）、风格（紫）、策略（青）。
  5. 「应用」按钮。
- 点击卡片本体 → 进入详情/编辑模式。
- 点击「应用」按钮 → 直接将 `content` 写入主输入框，关闭管理器（事件不冒泡到卡片）。

#### 3.6.3 筛选与搜索

- 顶部搜索框：支持按 Prompt 名称或内容做模糊搜索（不区分大小写）。
- 三个下拉：品类、风格、策略，多维 AND 组合筛选。
- 「重置」按钮：清空搜索词与三个下拉。
- 实时显示「共 N 条」结果统计。
- 无匹配结果时展示空状态提示。

#### 3.6.4 新增 / 编辑

- 「新建 Prompt」按钮 → 打开空白编辑器；点击卡片 → 打开当前 Prompt 编辑器。
- 编辑器字段：
  - 预览图上传框（120×120，点击触发文件选择，调用 `/api/upload-image` 上传，支持替换、移除）。
  - 名称（必填）、状态。
  - 品类、素材风格、内容策略下拉。
  - Prompt 内容文本域（必填）。
  - 操作按钮：取消、删除（仅编辑模式）、保存。
- 保存校验：名称、内容必填。
- 新增模式：写入数组首位 → 持久化 → 关闭编辑器 → 重新渲染列表。
- 编辑模式：原 ID 不变，更新字段后持久化 → 重新渲染。
- 删除：弹出二次确认，从数组中移除并持久化。

#### 3.6.5 应用 Prompt 到主输入框

- 入口 1：管理器中卡片底部「应用」按钮（关闭管理器）。
- 入口 2：在管理器外的 Prompt 选择器（快速浮层）中点击。
- 行为：
  1. 将 `content` 写入 `#universalPrompt` 文本域。
  2. 触发字符计数刷新。
  3. 主输入框边框短暂高亮（约 1.2s）作为视觉反馈。
  4. 弹出 Toast「Prompt 已应用」。

#### 3.6.6 老枚举值兼容

- 在加载 Prompt 时执行 `migrateLegacyPromptValues()`：
  - 将旧 `style` 值（如「高级感」「活泼」）按映射表迁移到新枚举。
  - 将旧 `strategy` 值（如「促销」「种草」）按映射表迁移到新枚举。
  - 迁移后写回 `localStorage`，确保现存数据不会失效。

### 3.7 F7 - 背景图比例

- 单选：Auto（默认）、1:1、3:4、9:16、16:9。
- 透传至后端 `bg_ratio`，最终作为 NanoBanana API 的 `aspectRatio` 参数控制生成尺寸。

### 3.8 F8 - 生成数量

- 单选：1（默认）/ 2 / 3 / 4 张。
- 指令生成 Tab 与仿写裂变 Tab 各自独立维护一组生成数量字段。

### 3.9 F9 - 商品 ID

- 仅在「仿写裂变」Tab 出现。
- 显示规则：
  - 模式为「仿写&裂变」或「仿写」时显示。
  - 模式为「裂变」时隐藏并不参与提交。
- 文本输入框，无格式校验（由业务后端处理合法性）。

### 3.10 F10 - 仿写裂变模式

- 单选：仿写&裂变（默认）、仿写、裂变。
- 切换模式时自动联动 F9 商品 ID 显隐。

### 3.11 F11 - 提交生成

#### 提交前校验

- **指令生成**：
  - 至少上传 1 张图片。
  - Prompt 不为空。
  - 必选项均已选择（默认值已满足）。
- **仿写裂变**：
  - 至少上传 1 张图片。
  - 「仿写&裂变」「仿写」模式下商品 ID 必填。

#### 调用流程（以指令生成为例）

1. 收集表单参数：`model`、`prompt`、`bg_ratio`、`image_urls` 等。
2. 中间面板替换为加载状态：显示图片数量、Prompt 摘要（前 100 字）、比例信息。
3. 弹出 Toast「开始生成」。
4. 前端 `POST /api/change-background`，请求体：
   ```json
   {
     "image_url": "数组首张",
     "image_urls": ["完整图片URL数组"],
     "prompt": "用户输入",
     "model": "nano-banana-fast",
     "bg_ratio": "Auto",
     "bg_method": "universal"
   }
   ```
5. 后端拼装 NanoBanana API 调用：
   - `urls` = `image_urls`
   - `prompt` = 用户原文
   - `aspectRatio` = `bg_ratio`
   - `model` = 选中的模型
6. 处理响应：
   - `status === 'succeeded'` 且包含 `output_url`：渲染结果图。
   - `status === 'processing'` 且包含 `task_id`：进入轮询逻辑（`pollUniversalCommandStatus`）。
   - 失败：中间面板显示错误信息 + 「重新生成」按钮。

### 3.12 F12 - 任务历史与回溯

#### 任务卡片展示

- 右侧任务列表按日期分组（今天 / 昨天 / 具体日期）。
- 每张任务卡片展示：
  - 主图（生成结果），左下角附原图缩略。
  - 右上角徽章：`指令生成` 或 `仿写裂变`。
  - 任务名称、生成时间、操作人（`@xxx`）。

#### 回溯逻辑（点击任务卡片）

1. 高亮当前选中卡片（蓝色边框）。
2. 关闭任务列表浮层。
3. 切换到目标 Tab（`generate` 或 `fission`）。
4. 按 Tab 回填字段：
   - 指令生成：模型、Prompt、背景比例。
   - 仿写裂变：商品 ID、模式、生成数量。
5. 回填上传图片：将历史 `uploadedImages` 写入 `universalUploadedImages`，调用统一渲染函数。
6. 顶部 Toast「已回溯任务：{标题}」。

#### 任务搜索与筛选

- 顶部支持按日期、任务 ID 模糊筛选；任一为空则不参与该维度过滤；全空则展示全部。

### 3.13 F13 - 重新生成

- 结果页右上角「重新生成」按钮 → 直接复用当前所有表单参数再次提交。
- 失败页同样提供「重新生成」按钮。

---

## 4. 接口契约

| 接口 | 方法 | 用途 |
|------|------|------|
| `/api/upload-image` | POST(multipart) | 上传图片（含 Prompt 预览图），返回云端或本地 URL |
| `/api/change-background` | POST(JSON) | 万能指令生成（指令生成 Tab） |
| `/api/check-task/:id` | GET | 查询异步任务状态（用于轮询） |

请求/响应字段以前端实际使用为准，详见各接口实现。

---

## 5. 数据存储

| 存储位置 | 键 | 说明 |
|---------|----|------|
| `localStorage` | `universalPrompts` | Prompt 库 JSON 数组，包含名称/内容/分类/预览图/状态等字段 |
| 服务器本地磁盘 | `uploads/` | 上传图片的临时落盘目录 |
| 腾讯云 COS | `nanobanana/web/` | 上传成功后的图片永久存储位置 |

---

## 6. 异常与边界

- 图片上传失败：Toast 提示具体文件名，已成功的图片不受影响。
- API 调用失败：中间面板展示错误信息 + 重新生成按钮，原表单不重置。
- 任务异步处理：`pollUniversalCommandStatus` 轮询任务状态直至结果产出或超时。
- 浏览器 `localStorage` 满载：保存 Prompt 时弹出原生 alert 提示。
- 无网络 / 后端未启动：上传与生成都会失败，Toast 提示「上传失败 / 生成失败」。

---

## 7. 附录：枚举值参考

### 7.1 品类
服饰 / 美妆 / 鞋包 / 家居 / 数码 / 食品

### 7.2 素材风格
质感简约风 / 原生实拍风 / 促销利益风 / 商品海报风 / 功能场景型

### 7.3 内容策略
上身版型类 / 场景穿搭类 / 细节质感类 / 对比差异类 / 新客利益类 / 套装礼盒类 / 单品展示类 / 多品矩阵类 / IP联名类 / 场景展示类 / 持妆功效类 / 上脸/上手实拍类

### 7.4 模型
nano-banana-fast（默认）/ nano-banana-pro / image-2

### 7.5 背景图比例
Auto（默认）/ 1:1 / 3:4 / 9:16 / 16:9

### 7.6 生成数量
1（默认）/ 2 / 3 / 4

### 7.7 仿写裂变模式
仿写&裂变（默认）/ 仿写 / 裂变
